# 本地打包编译提速（V1.4.1）

用户授权先提交剪贴板工作，再直接优化本地打包。

- 现有 `npm run tauri:build:local` 默认使用快速 Release 编译参数。
- 保留正式 release、开发模式、前端检查、四个二进制和安装包资源。
- 本地缓存独立，参数透传，失败退出码传播，不结束用户已有进程。
- 验收：参数/进程回归测试、CLI 冒烟、严格架构检查；未完成整包计时不得宣称实测提速。

# V1.4.0 导入与Skill同步执行计划

1. [x] 依赖extension-model-adapters的模型/目标身份/应用结果接口。
2. [x] 审阅父任务与本任务PRD；读领域规范并完成符号impact，获实施授权后start本子任务。
3. [x] 先导入预览，再staging/发布/部署/恢复；用隔离临时目录验证链接、复制、外部修改、部分失败和重试。
   GitHub安装同时验证地址/ref解析、候选发现、commit固定、下载取消/失败、归档边界和幂等；复用上述发布机制。
4. [x] 重复导入幂等；同名异内容可处理；来源不修改；缺失源/失效链接有解释；外部修改不误删；链接权限失败按所选策略处理。
5. [x] 按父任务implement.md运行适用的类型/Rust/架构及跨层检查，更新V1.4.0代码交付记录。
6. [x] 已记录平台验证状态：Windows 定向测试通过；当前环境无可用 WSL 发行版，且未提供 macOS 环境，WSL/macOS 实际验证待补；SSH 保持不支持且不触发本机写入。

审查修复记录：补齐本机/WSL 发布前后所有权复核、备份边界、归档/复制大小限制、WSL 发行版内父路径链接防护、无覆盖发布和卸载后的备份恢复记录；GitNexus 符号索引未包含本批新增文件，已执行逐符号查询并以契约、调用点和定向检查替代空影响结果。

失败仅恢复本任务有归属证据的输出；保留外部文件和活跃会话引用。
